# AIView Workflow Studio

AIView 多智能体工作流工作台 MVP。它会引导任务依次完成方向选择、脑图、六要素计划、作品创作和最终复盘。

## 环境要求

- Node.js 18 或更高版本
- npm

## 本地运行

```bash
npm install
npm run dev
```

打开 Vite 输出的本地地址即可访问。应用支持离线模式，会使用确定性备用逻辑；也可以在设置面板中连接兼容 OpenAI 接口的 API。

## 验证

```bash
npm test
npm run build
```
